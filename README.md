# Rust Dedicated Server (Windows)

Простой и понятный бат-файл для запуска **Rust Dedicated Server** под Windows.  
Подходит для новичков — не требует знаний Git, SteamCMD или консоли.

## 📦 Что внутри
- `rust_server.bat` — основной файл запуска
- Автоматическая загрузка SteamCMD
- Автоматическое обновление Rust сервера
- Работает на любом диске
- Минимум действий — максимум результата

---

## 🚀 Быстрый старт

1. Нажмите **Code → Download ZIP**
2. Распакуйте архив в любую папку
3. Откройте файл `rust_server.bat`
4. Измените блок **НАСТРОЙКИ СЕРВЕРА**
5. Сохраните файл и запустите его

При первом запуске:
- скачается SteamCMD
- установится Rust Dedicated Server
- сервер автоматически запустится

---

## ⚙️ Основные настройки

Редактируется **только верхняя часть файла**:

```bat
set "SERVER_NAME=My Rust Server"
set "SERVER_DESC=Welcome!"
set "SERVER_HEADER=https://example.com/rust_header.jpg"

set "SERVER_IDENTITY=rst"
set "MAX_PLAYERS=100"

set "SERVER_PORT=28015"
set "QUERY_PORT=28016"
set "RCON_PORT=28017"
set "APP_PORT=28082"

set "MAP_SEED=100816564"
set "MAP_SIZE=2500"
